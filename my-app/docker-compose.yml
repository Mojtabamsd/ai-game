#version: "3.9"

services:
  plankton-app:
    image: mojmas/plankton-ai:v1.3
    build: .
    ports:
      - "5000:5000"
    volumes:
      # Database persists here — mount a local folder to /data
      - ./data:/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped

